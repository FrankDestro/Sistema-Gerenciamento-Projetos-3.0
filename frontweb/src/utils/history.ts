import { createBrowserHistory } from "history";

/* Criado para poder fazer redirecionamento fora de componentes react, 
 pois as requisições estão em um arquivo ts (typescript que não são componentes reacts) */

export default createBrowserHistory();
